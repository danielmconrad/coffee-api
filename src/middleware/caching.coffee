cached = require 'cached'

module.exports = (app) ->

  # config = app.get('config')

  # return (req, res, next) -> next() unless config.cache.enabled

  # cache = cached 'kittens',
  #   backend:
  #     type: 'memcached',
  #     hosts: '127.0.0.1:11211'

  # (req, res, next) ->

  #   next()

  #   # originalSend = res.send

  #   # res.send = ->


